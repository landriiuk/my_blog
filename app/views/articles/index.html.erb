<header class="jumbotron text-center">
  <h1 class="display-5">Всі пости</h1>
  <p class="lead text-muted jumbo-width">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
</header>

<div class="row">
  <% @articles.each do |article| %>
    <div class="col-xs-12 col-md-6 col-xl-4 mb-4">
      <div class="card">
        <%= link_to article do %>
          <%= image_tag(article.image_url(:thumb), class: 'card-img-top') if article.image? %>
        <% end %>
        <div class="card-body">
          <h2 class="card-title"><%= link_to article.title, article %></h2>
          <<div>
            <%= truncate(article.text , length: 115) %>
          </div>

          <div class="d-flex justify-content-between align-items-center">
            <div class="btn-group">
              <%= link_to 'View', article, class: 'btn btn-sm btn-outline-primary' %>
              <%= link_to 'Edit', edit_article_path(article), class: 'btn btn-sm btn-outline-primary' %>
            </div>
            <small class="text-muted"><%= time_ago_in_words(article.updated_at) %></small>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
